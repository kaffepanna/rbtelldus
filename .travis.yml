language: ruby
rvm:
- 1.9.3
- '2.1'
- '2.2'
script: bundle exec rake test
before_deploy:
- bundle exec rake build
- export GEM_FILE="$(ls -1 pkg/*.gem | tail -n1)"
deploy:
  provider: rubygems
  api_key: $RUBYGEMS_KEY
  gem: rtelldus
  on:
    tags: true
    repo: kaffepanna/rtelldus
